#include "position.h"

// Constructor for Position class
Position::Position(int row, int column) {
    this->row = row;    // Assigns input row to object's row
    this->column = column; // Assigns input column to object's column
}