#pragma once

// A class that represents a position in the grid
class Position {
    public:
        Position(int row, int column); // Constructor for the Position class
        int row; // Row coordinate of the position
        int column; // Column coordinate of the position
    private:
};

